import { StyleSheet, View, FlatList } from 'react-native'
import React from 'react'

const MuestroGastos = () => {
  /*  const renderizarListaGastos = ({ unGasto, index }) => (
        <TouchableOpacity 
            style={styles.textContainers} 
            onPress={() => gestionarModal(index)} >
            <Text style={styles.texto_items}> {unGasto?.value} </Text>
        </TouchableOpacity>
    )

    return (
        <View style={styles.viewContainerItems}>
            <FlatList
                data={listaGastos}
                renderItem={renderizarListaGastos}
                keyExtractor={unGasto => unGasto.id}
            />
        </View>
    )*/
}

export default MuestroGastos

const styles = StyleSheet.create({

})